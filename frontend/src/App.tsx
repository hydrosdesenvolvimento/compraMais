/**
 * Componente raiz do scaffolding do compraMais.
 *
 * Placeholder funcional: as telas reais dependem do Design System do UX Expert
 * (precondicao registrada no System Design). Mantido minimo e testavel.
 */
export default function App() {
  return (
    <main data-cy="app-root">
      <h1>compraMais</h1>
      <p>Scaffolding do frontend SPA (React + Vite + TypeScript).</p>
    </main>
  );
}
